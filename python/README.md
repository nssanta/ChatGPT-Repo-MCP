# ChatGPT Repo MCP — Python package

This directory contains the Python implementation. Installation, configuration,
security, and tool documentation are shared by both implementations in the
repository-level [`README.md`](../README.md) and [`docs/`](../docs/) directory.
